<article>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    {% set published = post.data.published %}
    {% set modified = post.data.modified %}
    {% set tags = post.data.tags %}
    {% include "post-metadata.njk" %}
    
    <p>{{ post.data.description }}</p>
</article>